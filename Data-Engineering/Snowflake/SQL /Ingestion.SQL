CREATE OR REPLACE DATABASE DATA_ENG;
CREATE OR REPLACE SCHEMA WORKS;
CREATE OR REPLACE SCHEMA SOURCE;


-- Creating Stage for Ingestion
CREATE OR REPLACE STAGE my_int_stage
  ENCRYPTION = (TYPE = 'SNOWFLAKE_SSE'),
    DIRECTORY = (ENABLE = TRUE)
 
;

